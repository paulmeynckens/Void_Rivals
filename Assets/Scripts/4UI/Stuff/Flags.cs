using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using Core;

namespace UI.Stuff

{
    public static class Flags
    {
        /*
        public static Image Flag(Language language)
        {
            switch (language)
            {
                case Language.english:
                    return Resources.Load("English") as Image;
                case Language.français:
                    return Resources.Load("Francais") as Image;
                case Language.русский:
                    return Resources.Load("Russian") as Image;
                case Language.deutche:
                    return Resources.Load("German") as Image;
                case Language.español:
                    return Resources.Load("Espanol") as Image;
                default:
                    return Resources.Load("Default") as Image;

            }
        }
        */
    }
}

