using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace PoloGames
{
    public class CrewChatManager : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
